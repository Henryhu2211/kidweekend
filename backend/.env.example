# ===========================================
# KidWeekend Backend 环境变量模板
# ===========================================

# Database (Neon PostgreSQL) — 已创建
DATABASE_URL="postgresql://user:password@host:5432/kidweekend?sslmode=require"

# Redis (Upstash) — 已创建
REDIS_HOST="pro-grouse-81397.upstash.io"
REDIS_PORT=6379
REDIS_PASSWORD="your-redis-password"

# Auth (请生成32位以上随机字符串)
JWT_SECRET="generate-a-random-32-char-secret-here"
AUTH_SECRET="generate-another-random-32-char-secret-here"

# App
PORT=3000
NODE_ENV=production
